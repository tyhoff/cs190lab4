public class Watch {
	
	int num;
	int trash;
	Functions wm;
	
	public Watch() {
		num = 5;
		trash = 0;
		wm = new Functions();
	}
	
	public void begin() {
		
		System.out.format("Num Value Before: %d\n", num);
		
		if (wm.function1()) num *= 2; else trash *= num ;
		if (wm.function2()) num *= 2; else trash *= num ;
		if (wm.function3()) num *= 2; else trash *= num ;
		if (wm.function4()) num *= 2; else trash *= num ;
		if (wm.function5()) num *= 2; else trash *= num ;
		if (wm.function6()) num *= 2; else trash *= num ;
		if (wm.function7()) num *= 2; else trash *= num ;
		if (wm.function8()) num *= 2; else trash *= num ;
		if (wm.function9()) num *= 2; else trash *= num ;
		if (wm.function10()) num *= 2; else trash *= num ;
		if (wm.function11()) num *= 2; else trash *= num ;
		if (wm.function12()) num *= 2; else trash *= num ;
		if (wm.function13()) num *= 2; else trash *= num ;
		if (wm.function14()) num *= 2; else trash *= num ;
		if (wm.function15()) num *= 2; else trash *= num ;
		if (wm.function16()) num *= 2; else trash *= num ;
		if (wm.function17()) num *= 2; else trash *= num ;
		if (wm.function18()) num *= 2; else trash *= num ;
		if (wm.function19()) num *= 2; else trash *= num ;
		if (wm.function20()) num *= 2; else trash *= num ;
		if (wm.function21()) num *= 2; else trash *= num ;
		if (wm.function22()) num *= 2; else trash *= num ;
		if (wm.function23()) num *= 2; else trash *= num ;
		if (wm.function24()) num *= 2; else trash *= num ;
		if (wm.function25()) num *= 2; else trash *= num ;
		if (wm.function26()) num *= 2; else trash *= num ;
		if (wm.function27()) num *= 2; else trash *= num ;
		if (wm.function28()) num *= 2; else trash *= num ;
		if (wm.function29()) num *= 2; else trash *= num ;
		if (wm.function30()) num *= 2; else trash *= num ;
		if (wm.function31()) num *= 2; else trash *= num ;
		if (wm.function32()) num *= 2; else trash *= num ;
		if (wm.function33()) num *= 2; else trash *= num ;
		if (wm.function34()) num *= 2; else trash *= num ;
		if (wm.function35()) num *= 2; else trash *= num ;
		if (wm.function36()) num *= 2; else trash *= num ;
		if (wm.function37()) num *= 2; else trash *= num ;
		if (wm.function38()) num *= 2; else trash *= num ;
		if (wm.function39()) num *= 2; else trash *= num ;
		if (wm.function40()) num *= 2; else trash *= num ;
		if (wm.function41()) num *= 2; else trash *= num ;
		if (wm.function42()) num *= 2; else trash *= num ;
		if (wm.function43()) num *= 2; else trash *= num ;
		if (wm.function44()) num *= 2; else trash *= num ;
		if (wm.function45()) num *= 2; else trash *= num ;
		if (wm.function46()) num *= 2; else trash *= num ;
		if (wm.function47()) num *= 2; else trash *= num ;
		if (wm.function48()) num *= 2; else trash *= num ;
		if (wm.function49()) num *= 2; else trash *= num ;
		if (wm.function50()) num *= 2; else trash *= num ;
		if (wm.function51()) num *= 2; else trash *= num ;
		if (wm.function52()) num *= 2; else trash *= num ;
		if (wm.function53()) num *= 2; else trash *= num ;
		if (wm.function54()) num *= 2; else trash *= num ;
		if (wm.function55()) num *= 2; else trash *= num ;
		if (wm.function56()) num *= 2; else trash *= num ;
		if (wm.function57()) num *= 2; else trash *= num ;
		if (wm.function58()) num *= 2; else trash *= num ;
		if (wm.function59()) num *= 2; else trash *= num ;
		if (wm.function60()) num *= 2; else trash *= num ;
		if (wm.function61()) num *= 2; else trash *= num ;
		if (wm.function62()) num *= 2; else trash *= num ;
		if (wm.function63()) num *= 2; else trash *= num ;
		if (wm.function64()) num *= 2; else trash *= num ;
		if (wm.function65()) num *= 2; else trash *= num ;
		if (wm.function66()) num *= 2; else trash *= num ;
		if (wm.function67()) num *= 2; else trash *= num ;
		if (wm.function68()) num *= 2; else trash *= num ;
		if (wm.function69()) num *= 2; else trash *= num ;
		if (wm.function70()) num *= 2; else trash *= num ;
		if (wm.function71()) num *= 2; else trash *= num ;
		if (wm.function72()) num *= 2; else trash *= num ;
		if (wm.function73()) num *= 2; else trash *= num ;
		if (wm.function74()) num *= 2; else trash *= num ;
		if (wm.function75()) num *= 2; else trash *= num ;
		if (wm.function76()) num *= 2; else trash *= num ;
		if (wm.function77()) num *= 2; else trash *= num ;
		if (wm.function78()) num *= 2; else trash *= num ;
		if (wm.function79()) num *= 2; else trash *= num ;
		if (wm.function80()) num *= 2; else trash *= num ;
		if (wm.function81()) num *= 2; else trash *= num ;
		if (wm.function82()) num *= 2; else trash *= num ;
		if (wm.function83()) num *= 2; else trash *= num ;
		if (wm.function84()) num *= 2; else trash *= num ;
		if (wm.function85()) num *= 2; else trash *= num ;
		if (wm.function86()) num *= 2; else trash *= num ;
		if (wm.function87()) num *= 2; else trash *= num ;
		if (wm.function88()) num *= 2; else trash *= num ;
		if (wm.function89()) num *= 2; else trash *= num ;
		if (wm.function90()) num *= 2; else trash *= num ;
		if (wm.function91()) num *= 2; else trash *= num ;
		if (wm.function92()) num *= 2; else trash *= num ;
		if (wm.function93()) num *= 2; else trash *= num ;
		if (wm.function94()) num *= 2; else trash *= num ;
		if (wm.function95()) num *= 2; else trash *= num ;
		if (wm.function96()) num *= 2; else trash *= num ;
		if (wm.function97()) num *= 2; else trash *= num ;
		if (wm.function98()) num *= 2; else trash *= num ;
		if (wm.function99()) num *= 2; else trash *= num ;
		if (wm.function100()) num *= 2; else trash *= num ;
		if (wm.function101()) num *= 2; else trash *= num ;
		if (wm.function102()) num *= 2; else trash *= num ;
		if (wm.function103()) num *= 2; else trash *= num ;
		if (wm.function104()) num *= 2; else trash *= num ;
		if (wm.function105()) num *= 2; else trash *= num ;
		if (wm.function106()) num *= 2; else trash *= num ;
		if (wm.function107()) num *= 2; else trash *= num ;
		if (wm.function108()) num *= 2; else trash *= num ;
		if (wm.function109()) num *= 2; else trash *= num ;
		if (wm.function110()) num *= 2; else trash *= num ;
		if (wm.function111()) num *= 2; else trash *= num ;
		if (wm.function112()) num *= 2; else trash *= num ;
		if (wm.function113()) num *= 2; else trash *= num ;
		if (wm.function114()) num *= 2; else trash *= num ;
		if (wm.function115()) num *= 2; else trash *= num ;
		if (wm.function116()) num *= 2; else trash *= num ;
		if (wm.function117()) num *= 2; else trash *= num ;
		if (wm.function118()) num *= 2; else trash *= num ;
		if (wm.function119()) num *= 2; else trash *= num ;
		if (wm.function120()) num *= 2; else trash *= num ;
		if (wm.function121()) num *= 2; else trash *= num ;
		if (wm.function122()) num *= 2; else trash *= num ;
		if (wm.function123()) num *= 2; else trash *= num ;
		if (wm.function124()) num *= 2; else trash *= num ;
		if (wm.function125()) num *= 2; else trash *= num ;
		if (wm.function126()) num *= 2; else trash *= num ;
		if (wm.function127()) num *= 2; else trash *= num ;
		if (wm.function128()) num *= 2; else trash *= num ;
		if (wm.function129()) num *= 2; else trash *= num ;
		if (wm.function130()) num *= 2; else trash *= num ;
		if (wm.function131()) num *= 2; else trash *= num ;
		if (wm.function132()) num *= 2; else trash *= num ;
		if (wm.function133()) num *= 2; else trash *= num ;
		if (wm.function134()) num *= 2; else trash *= num ;
		if (wm.function135()) num *= 2; else trash *= num ;
		if (wm.function136()) num *= 2; else trash *= num ;
		if (wm.function137()) num *= 2; else trash *= num ;
		if (wm.function138()) num *= 2; else trash *= num ;
		if (wm.function139()) num *= 2; else trash *= num ;
		if (wm.function140()) num *= 2; else trash *= num ;
		if (wm.function141()) num *= 2; else trash *= num ;
		if (wm.function142()) num *= 2; else trash *= num ;
		if (wm.function143()) num *= 2; else trash *= num ;
		if (wm.function144()) num *= 2; else trash *= num ;
		if (wm.function145()) num *= 2; else trash *= num ;
		if (wm.function146()) num *= 2; else trash *= num ;
		if (wm.function147()) num *= 2; else trash *= num ;
		if (wm.function148()) num *= 2; else trash *= num ;
		if (wm.function149()) num *= 2; else trash *= num ;
		if (wm.function150()) num *= 2; else trash *= num ;
		if (wm.function151()) num *= 2; else trash *= num ;
		if (wm.function152()) num *= 2; else trash *= num ;
		if (wm.function153()) num *= 2; else trash *= num ;
		if (wm.function154()) num *= 2; else trash *= num ;
		if (wm.function155()) num *= 2; else trash *= num ;
		if (wm.function156()) num *= 2; else trash *= num ;
		if (wm.function157()) num *= 2; else trash *= num ;
		if (wm.function158()) num *= 2; else trash *= num ;
		if (wm.function159()) num *= 2; else trash *= num ;
		if (wm.function160()) num *= 2; else trash *= num ;
		if (wm.function161()) num *= 2; else trash *= num ;
		if (wm.function162()) num *= 2; else trash *= num ;
		if (wm.function163()) num *= 2; else trash *= num ;
		if (wm.function164()) num *= 2; else trash *= num ;
		if (wm.function165()) num *= 2; else trash *= num ;
		if (wm.function166()) num *= 2; else trash *= num ;
		if (wm.function167()) num *= 2; else trash *= num ;
		if (wm.function168()) num *= 2; else trash *= num ;
		if (wm.function169()) num *= 2; else trash *= num ;
		if (wm.function170()) num *= 2; else trash *= num ;
		if (wm.function171()) num *= 2; else trash *= num ;
		if (wm.function172()) num *= 2; else trash *= num ;
		if (wm.function173()) num *= 2; else trash *= num ;
		if (wm.function174()) num *= 2; else trash *= num ;
		if (wm.function175()) num *= 2; else trash *= num ;
		if (wm.function176()) num *= 2; else trash *= num ;
		if (wm.function177()) num *= 2; else trash *= num ;
		if (wm.function178()) num *= 2; else trash *= num ;
		if (wm.function179()) num *= 2; else trash *= num ;
		if (wm.function180()) num *= 2; else trash *= num ;
		if (wm.function181()) num *= 2; else trash *= num ;
		if (wm.function182()) num *= 2; else trash *= num ;
		if (wm.function183()) num *= 2; else trash *= num ;
		if (wm.function184()) num *= 2; else trash *= num ;
		if (wm.function185()) num *= 2; else trash *= num ;
		if (wm.function186()) num *= 2; else trash *= num ;
		if (wm.function187()) num *= 2; else trash *= num ;
		if (wm.function188()) num *= 2; else trash *= num ;
		if (wm.function189()) num *= 2; else trash *= num ;
		if (wm.function190()) num *= 2; else trash *= num ;
		if (wm.function191()) num *= 2; else trash *= num ;
		if (wm.function192()) num *= 2; else trash *= num ;
		if (wm.function193()) num *= 2; else trash *= num ;
		if (wm.function194()) num *= 2; else trash *= num ;
		if (wm.function195()) num *= 2; else trash *= num ;
		if (wm.function196()) num *= 2; else trash *= num ;
		if (wm.function197()) num *= 2; else trash *= num ;
		if (wm.function198()) num *= 2; else trash *= num ;
		if (wm.function199()) num *= 2; else trash *= num ;
		if (wm.function200()) num *= 2; else trash *= num ;
		if (wm.function201()) num *= 2; else trash *= num ;
		if (wm.function202()) num *= 2; else trash *= num ;
		if (wm.function203()) num *= 2; else trash *= num ;
		if (wm.function204()) num *= 2; else trash *= num ;
		if (wm.function205()) num *= 2; else trash *= num ;
		if (wm.function206()) num *= 2; else trash *= num ;
		if (wm.function207()) num *= 2; else trash *= num ;
		if (wm.function208()) num *= 2; else trash *= num ;
		if (wm.function209()) num *= 2; else trash *= num ;
		if (wm.function210()) num *= 2; else trash *= num ;
		if (wm.function211()) num *= 2; else trash *= num ;
		if (wm.function212()) num *= 2; else trash *= num ;
		if (wm.function213()) num *= 2; else trash *= num ;
		if (wm.function214()) num *= 2; else trash *= num ;
		if (wm.function215()) num *= 2; else trash *= num ;
		if (wm.function216()) num *= 2; else trash *= num ;
		if (wm.function217()) num *= 2; else trash *= num ;
		if (wm.function218()) num *= 2; else trash *= num ;
		if (wm.function219()) num *= 2; else trash *= num ;
		if (wm.function220()) num *= 2; else trash *= num ;
		if (wm.function221()) num *= 2; else trash *= num ;
		if (wm.function222()) num *= 2; else trash *= num ;
		if (wm.function223()) num *= 2; else trash *= num ;
		if (wm.function224()) num *= 2; else trash *= num ;
		if (wm.function225()) num *= 2; else trash *= num ;
		if (wm.function226()) num *= 2; else trash *= num ;
		if (wm.function227()) num *= 2; else trash *= num ;
		if (wm.function228()) num *= 2; else trash *= num ;
		if (wm.function229()) num *= 2; else trash *= num ;
		if (wm.function230()) num *= 2; else trash *= num ;
		if (wm.function231()) num *= 2; else trash *= num ;
		if (wm.function232()) num *= 2; else trash *= num ;
		if (wm.function233()) num *= 2; else trash *= num ;
		if (wm.function234()) num *= 2; else trash *= num ;
		if (wm.function235()) num *= 2; else trash *= num ;
		if (wm.function236()) num *= 2; else trash *= num ;
		if (wm.function237()) num *= 2; else trash *= num ;
		if (wm.function238()) num *= 2; else trash *= num ;
		if (wm.function239()) num *= 2; else trash *= num ;
		if (wm.function240()) num *= 2; else trash *= num ;
		if (wm.function241()) num *= 2; else trash *= num ;
		if (wm.function242()) num *= 2; else trash *= num ;
		if (wm.function243()) num *= 2; else trash *= num ;
		if (wm.function244()) num *= 2; else trash *= num ;
		if (wm.function245()) num *= 2; else trash *= num ;
		if (wm.function246()) num *= 2; else trash *= num ;
		if (wm.function247()) num *= 2; else trash *= num ;
		if (wm.function248()) num *= 2; else trash *= num ;
		if (wm.function249()) num *= 2; else trash *= num ;
		if (wm.function250()) num *= 2; else trash *= num ;
		if (wm.function251()) num *= 2; else trash *= num ;
		if (wm.function252()) num *= 2; else trash *= num ;
		if (wm.function253()) num *= 2; else trash *= num ;
		if (wm.function254()) num *= 2; else trash *= num ;
		if (wm.function255()) num *= 2; else trash *= num ;
		if (wm.function256()) num *= 2; else trash *= num ;
		if (wm.function257()) num *= 2; else trash *= num ;
		if (wm.function258()) num *= 2; else trash *= num ;
		if (wm.function259()) num *= 2; else trash *= num ;
		if (wm.function260()) num *= 2; else trash *= num ;
		if (wm.function261()) num *= 2; else trash *= num ;
		if (wm.function262()) num *= 2; else trash *= num ;
		if (wm.function263()) num *= 2; else trash *= num ;
		if (wm.function264()) num *= 2; else trash *= num ;
		if (wm.function265()) num *= 2; else trash *= num ;
		if (wm.function266()) num *= 2; else trash *= num ;
		if (wm.function267()) num *= 2; else trash *= num ;
		if (wm.function268()) num *= 2; else trash *= num ;
		if (wm.function269()) num *= 2; else trash *= num ;
		if (wm.function270()) num *= 2; else trash *= num ;
		if (wm.function271()) num *= 2; else trash *= num ;
		if (wm.function272()) num *= 2; else trash *= num ;
		if (wm.function273()) num *= 2; else trash *= num ;
		if (wm.function274()) num *= 2; else trash *= num ;
		if (wm.function275()) num *= 2; else trash *= num ;
		if (wm.function276()) num *= 2; else trash *= num ;
		if (wm.function277()) num *= 2; else trash *= num ;
		if (wm.function278()) num *= 2; else trash *= num ;
		if (wm.function279()) num *= 2; else trash *= num ;
		if (wm.function280()) num *= 2; else trash *= num ;
		if (wm.function281()) num *= 2; else trash *= num ;
		if (wm.function282()) num *= 2; else trash *= num ;
		if (wm.function283()) num *= 2; else trash *= num ;
		if (wm.function284()) num *= 2; else trash *= num ;
		if (wm.function285()) num *= 2; else trash *= num ;
		if (wm.function286()) num *= 2; else trash *= num ;
		if (wm.function287()) num *= 2; else trash *= num ;
		if (wm.function288()) num *= 2; else trash *= num ;
		if (wm.function289()) num *= 2; else trash *= num ;
		if (wm.function290()) num *= 2; else trash *= num ;
		if (wm.function291()) num *= 2; else trash *= num ;
		if (wm.function292()) num *= 2; else trash *= num ;
		if (wm.function293()) num *= 2; else trash *= num ;
		if (wm.function294()) num *= 2; else trash *= num ;
		if (wm.function295()) num *= 2; else trash *= num ;
		if (wm.function296()) num *= 2; else trash *= num ;
		if (wm.function297()) num *= 2; else trash *= num ;
		if (wm.function298()) num *= 2; else trash *= num ;
		if (wm.function299()) num *= 2; else trash *= num ;
		if (wm.function300()) num *= 2; else trash *= num ;
		if (wm.function301()) num *= 2; else trash *= num ;
		if (wm.function302()) num *= 2; else trash *= num ;
		if (wm.function303()) num *= 2; else trash *= num ;
		if (wm.function304()) num *= 2; else trash *= num ;
		if (wm.function305()) num *= 2; else trash *= num ;
		if (wm.function306()) num *= 2; else trash *= num ;
		if (wm.function307()) num *= 2; else trash *= num ;
		if (wm.function308()) num *= 2; else trash *= num ;
		if (wm.function309()) num *= 2; else trash *= num ;
		if (wm.function310()) num *= 2; else trash *= num ;
		if (wm.function311()) num *= 2; else trash *= num ;
		if (wm.function312()) num *= 2; else trash *= num ;
		if (wm.function313()) num *= 2; else trash *= num ;
		if (wm.function314()) num *= 2; else trash *= num ;
		if (wm.function315()) num *= 2; else trash *= num ;
		if (wm.function316()) num *= 2; else trash *= num ;
		if (wm.function317()) num *= 2; else trash *= num ;
		if (wm.function318()) num *= 2; else trash *= num ;
		if (wm.function319()) num *= 2; else trash *= num ;
		if (wm.function320()) num *= 2; else trash *= num ;
		if (wm.function321()) num *= 2; else trash *= num ;
		if (wm.function322()) num *= 2; else trash *= num ;
		if (wm.function323()) num *= 2; else trash *= num ;
		if (wm.function324()) num *= 2; else trash *= num ;
		if (wm.function325()) num *= 2; else trash *= num ;
		if (wm.function326()) num *= 2; else trash *= num ;
		if (wm.function327()) num *= 2; else trash *= num ;
		if (wm.function328()) num *= 2; else trash *= num ;
		if (wm.function329()) num *= 2; else trash *= num ;
		if (wm.function330()) num *= 2; else trash *= num ;
		if (wm.function331()) num *= 2; else trash *= num ;
		if (wm.function332()) num *= 2; else trash *= num ;
		if (wm.function333()) num *= 2; else trash *= num ;
		if (wm.function334()) num *= 2; else trash *= num ;
		if (wm.function335()) num *= 2; else trash *= num ;
		if (wm.function336()) num *= 2; else trash *= num ;
		if (wm.function337()) num *= 2; else trash *= num ;
		if (wm.function338()) num *= 2; else trash *= num ;
		if (wm.function339()) num *= 2; else trash *= num ;
		if (wm.function340()) num *= 2; else trash *= num ;
		if (wm.function341()) num *= 2; else trash *= num ;
		if (wm.function342()) num *= 2; else trash *= num ;
		if (wm.function343()) num *= 2; else trash *= num ;
		if (wm.function344()) num *= 2; else trash *= num ;
		if (wm.function345()) num *= 2; else trash *= num ;
		if (wm.function346()) num *= 2; else trash *= num ;
		if (wm.function347()) num *= 2; else trash *= num ;
		if (wm.function348()) num *= 2; else trash *= num ;
		if (wm.function349()) num *= 2; else trash *= num ;
		if (wm.function350()) num *= 2; else trash *= num ;
		if (wm.function351()) num *= 2; else trash *= num ;
		if (wm.function352()) num *= 2; else trash *= num ;
		if (wm.function353()) num *= 2; else trash *= num ;
		if (wm.function354()) num *= 2; else trash *= num ;
		if (wm.function355()) num *= 2; else trash *= num ;
		if (wm.function356()) num *= 2; else trash *= num ;
		if (wm.function357()) num *= 2; else trash *= num ;
		if (wm.function358()) num *= 2; else trash *= num ;
		if (wm.function359()) num *= 2; else trash *= num ;
		if (wm.function360()) num *= 2; else trash *= num ;
		if (wm.function361()) num *= 2; else trash *= num ;
		if (wm.function362()) num *= 2; else trash *= num ;
		if (wm.function363()) num *= 2; else trash *= num ;
		if (wm.function364()) num *= 2; else trash *= num ;
		if (wm.function365()) num *= 2; else trash *= num ;
		if (wm.function366()) num *= 2; else trash *= num ;
		if (wm.function367()) num *= 2; else trash *= num ;
		if (wm.function368()) num *= 2; else trash *= num ;
		if (wm.function369()) num *= 2; else trash *= num ;
		if (wm.function370()) num *= 2; else trash *= num ;
		if (wm.function371()) num *= 2; else trash *= num ;
		if (wm.function372()) num *= 2; else trash *= num ;
		if (wm.function373()) num *= 2; else trash *= num ;
		if (wm.function374()) num *= 2; else trash *= num ;
		if (wm.function375()) num *= 2; else trash *= num ;
		if (wm.function376()) num *= 2; else trash *= num ;
		if (wm.function377()) num *= 2; else trash *= num ;
		if (wm.function378()) num *= 2; else trash *= num ;
		if (wm.function379()) num *= 2; else trash *= num ;
		if (wm.function380()) num *= 2; else trash *= num ;
		if (wm.function381()) num *= 2; else trash *= num ;
		if (wm.function382()) num *= 2; else trash *= num ;
		if (wm.function383()) num *= 2; else trash *= num ;
		if (wm.function384()) num *= 2; else trash *= num ;
		if (wm.function385()) num *= 2; else trash *= num ;
		if (wm.function386()) num *= 2; else trash *= num ;
		if (wm.function387()) num *= 2; else trash *= num ;
		if (wm.function388()) num *= 2; else trash *= num ;
		if (wm.function389()) num *= 2; else trash *= num ;
		if (wm.function390()) num *= 2; else trash *= num ;
		if (wm.function391()) num *= 2; else trash *= num ;
		if (wm.function392()) num *= 2; else trash *= num ;
		if (wm.function393()) num *= 2; else trash *= num ;
		if (wm.function394()) num *= 2; else trash *= num ;
		if (wm.function395()) num *= 2; else trash *= num ;
		if (wm.function396()) num *= 2; else trash *= num ;
		if (wm.function397()) num *= 2; else trash *= num ;
		if (wm.function398()) num *= 2; else trash *= num ;
		if (wm.function399()) num *= 2; else trash *= num ;
		if (wm.function400()) num *= 2; else trash *= num ;
		if (wm.function401()) num *= 2; else trash *= num ;
		if (wm.function402()) num *= 2; else trash *= num ;
		if (wm.function403()) num *= 2; else trash *= num ;
		if (wm.function404()) num *= 2; else trash *= num ;
		if (wm.function405()) num *= 2; else trash *= num ;
		if (wm.function406()) num *= 2; else trash *= num ;
		if (wm.function407()) num *= 2; else trash *= num ;
		if (wm.function408()) num *= 2; else trash *= num ;
		if (wm.function409()) num *= 2; else trash *= num ;
		if (wm.function410()) num *= 2; else trash *= num ;
		if (wm.function411()) num *= 2; else trash *= num ;
		if (wm.function412()) num *= 2; else trash *= num ;
		if (wm.function413()) num *= 2; else trash *= num ;
		if (wm.function414()) num *= 2; else trash *= num ;
		if (wm.function415()) num *= 2; else trash *= num ;
		if (wm.function416()) num *= 2; else trash *= num ;
		if (wm.function417()) num *= 2; else trash *= num ;
		if (wm.function418()) num *= 2; else trash *= num ;
		if (wm.function419()) num *= 2; else trash *= num ;
		if (wm.function420()) num *= 2; else trash *= num ;
		if (wm.function421()) num *= 2; else trash *= num ;
		if (wm.function422()) num *= 2; else trash *= num ;
		if (wm.function423()) num *= 2; else trash *= num ;
		if (wm.function424()) num *= 2; else trash *= num ;
		if (wm.function425()) num *= 2; else trash *= num ;
		if (wm.function426()) num *= 2; else trash *= num ;
		if (wm.function427()) num *= 2; else trash *= num ;
		if (wm.function428()) num *= 2; else trash *= num ;
		if (wm.function429()) num *= 2; else trash *= num ;
		if (wm.function430()) num *= 2; else trash *= num ;
		if (wm.function431()) num *= 2; else trash *= num ;
		if (wm.function432()) num *= 2; else trash *= num ;
		if (wm.function433()) num *= 2; else trash *= num ;
		if (wm.function434()) num *= 2; else trash *= num ;
		if (wm.function435()) num *= 2; else trash *= num ;
		if (wm.function436()) num *= 2; else trash *= num ;
		if (wm.function437()) num *= 2; else trash *= num ;
		if (wm.function438()) num *= 2; else trash *= num ;
		if (wm.function439()) num *= 2; else trash *= num ;
		if (wm.function440()) num *= 2; else trash *= num ;
		if (wm.function441()) num *= 2; else trash *= num ;
		if (wm.function442()) num *= 2; else trash *= num ;
		if (wm.function443()) num *= 2; else trash *= num ;
		if (wm.function444()) num *= 2; else trash *= num ;
		if (wm.function445()) num *= 2; else trash *= num ;
		if (wm.function446()) num *= 2; else trash *= num ;
		if (wm.function447()) num *= 2; else trash *= num ;
		if (wm.function448()) num *= 2; else trash *= num ;
		if (wm.function449()) num *= 2; else trash *= num ;
		if (wm.function450()) num *= 2; else trash *= num ;
		if (wm.function451()) num *= 2; else trash *= num ;
		if (wm.function452()) num *= 2; else trash *= num ;
		if (wm.function453()) num *= 2; else trash *= num ;
		if (wm.function454()) num *= 2; else trash *= num ;
		if (wm.function455()) num *= 2; else trash *= num ;
		if (wm.function456()) num *= 2; else trash *= num ;
		if (wm.function457()) num *= 2; else trash *= num ;
		if (wm.function458()) num *= 2; else trash *= num ;
		if (wm.function459()) num *= 2; else trash *= num ;
		if (wm.function460()) num *= 2; else trash *= num ;
		if (wm.function461()) num *= 2; else trash *= num ;
		if (wm.function462()) num *= 2; else trash *= num ;
		if (wm.function463()) num *= 2; else trash *= num ;
		if (wm.function464()) num *= 2; else trash *= num ;
		if (wm.function465()) num *= 2; else trash *= num ;
		if (wm.function466()) num *= 2; else trash *= num ;
		if (wm.function467()) num *= 2; else trash *= num ;
		if (wm.function468()) num *= 2; else trash *= num ;
		if (wm.function469()) num *= 2; else trash *= num ;
		if (wm.function470()) num *= 2; else trash *= num ;
		if (wm.function471()) num *= 2; else trash *= num ;
		if (wm.function472()) num *= 2; else trash *= num ;
		if (wm.function473()) num *= 2; else trash *= num ;
		if (wm.function474()) num *= 2; else trash *= num ;
		if (wm.function475()) num *= 2; else trash *= num ;
		if (wm.function476()) num *= 2; else trash *= num ;
		if (wm.function477()) num *= 2; else trash *= num ;
		if (wm.function478()) num *= 2; else trash *= num ;
		if (wm.function479()) num *= 2; else trash *= num ;
		if (wm.function480()) num *= 2; else trash *= num ;
		if (wm.function481()) num *= 2; else trash *= num ;
		if (wm.function482()) num *= 2; else trash *= num ;
		if (wm.function483()) num *= 2; else trash *= num ;
		if (wm.function484()) num *= 2; else trash *= num ;
		if (wm.function485()) num *= 2; else trash *= num ;
		if (wm.function486()) num *= 2; else trash *= num ;
		if (wm.function487()) num *= 2; else trash *= num ;
		if (wm.function488()) num *= 2; else trash *= num ;
		if (wm.function489()) num *= 2; else trash *= num ;
		if (wm.function490()) num *= 2; else trash *= num ;
		if (wm.function491()) num *= 2; else trash *= num ;
		if (wm.function492()) num *= 2; else trash *= num ;
		if (wm.function493()) num *= 2; else trash *= num ;
		if (wm.function494()) num *= 2; else trash *= num ;
		if (wm.function495()) num *= 2; else trash *= num ;
		if (wm.function496()) num *= 2; else trash *= num ;
		if (wm.function497()) num *= 2; else trash *= num ;
		if (wm.function498()) num *= 2; else trash *= num ;
		if (wm.function499()) num *= 2; else trash *= num ;
		if (wm.function500()) num *= 2; else trash *= num ;
		if (wm.function501()) num *= 2; else trash *= num ;
		if (wm.function502()) num *= 2; else trash *= num ;
		if (wm.function503()) num *= 2; else trash *= num ;
		if (wm.function504()) num *= 2; else trash *= num ;
		if (wm.function505()) num *= 2; else trash *= num ;
		if (wm.function506()) num *= 2; else trash *= num ;
		if (wm.function507()) num *= 2; else trash *= num ;
		if (wm.function508()) num *= 2; else trash *= num ;
		if (wm.function509()) num *= 2; else trash *= num ;
		if (wm.function510()) num *= 2; else trash *= num ;
		if (wm.function511()) num *= 2; else trash *= num ;
		if (wm.function512()) num *= 2; else trash *= num ;
		if (wm.function513()) num *= 2; else trash *= num ;
		if (wm.function514()) num *= 2; else trash *= num ;
		if (wm.function515()) num *= 2; else trash *= num ;
		if (wm.function516()) num *= 2; else trash *= num ;
		if (wm.function517()) num *= 2; else trash *= num ;
		if (wm.function518()) num *= 2; else trash *= num ;
		if (wm.function519()) num *= 2; else trash *= num ;
		if (wm.function520()) num *= 2; else trash *= num ;
		if (wm.function521()) num *= 2; else trash *= num ;
		if (wm.function522()) num *= 2; else trash *= num ;
		if (wm.function523()) num *= 2; else trash *= num ;
		if (wm.function524()) num *= 2; else trash *= num ;
		if (wm.function525()) num *= 2; else trash *= num ;
		if (wm.function526()) num *= 2; else trash *= num ;
		if (wm.function527()) num *= 2; else trash *= num ;
		if (wm.function528()) num *= 2; else trash *= num ;
		if (wm.function529()) num *= 2; else trash *= num ;
		if (wm.function530()) num *= 2; else trash *= num ;
		if (wm.function531()) num *= 2; else trash *= num ;
		if (wm.function532()) num *= 2; else trash *= num ;
		if (wm.function533()) num *= 2; else trash *= num ;
		if (wm.function534()) num *= 2; else trash *= num ;
		if (wm.function535()) num *= 2; else trash *= num ;
		if (wm.function536()) num *= 2; else trash *= num ;
		if (wm.function537()) num *= 2; else trash *= num ;
		if (wm.function538()) num *= 2; else trash *= num ;
		if (wm.function539()) num *= 2; else trash *= num ;
		if (wm.function540()) num *= 2; else trash *= num ;
		if (wm.function541()) num *= 2; else trash *= num ;
		if (wm.function542()) num *= 2; else trash *= num ;
		if (wm.function543()) num *= 2; else trash *= num ;
		if (wm.function544()) num *= 2; else trash *= num ;
		if (wm.function545()) num *= 2; else trash *= num ;
		if (wm.function546()) num *= 2; else trash *= num ;
		if (wm.function547()) num *= 2; else trash *= num ;
		if (wm.function548()) num *= 2; else trash *= num ;
		if (wm.function549()) num *= 2; else trash *= num ;
		if (wm.function550()) num *= 2; else trash *= num ;
		if (wm.function551()) num *= 2; else trash *= num ;
		if (wm.function552()) num *= 2; else trash *= num ;
		if (wm.function553()) num *= 2; else trash *= num ;
		if (wm.function554()) num *= 2; else trash *= num ;
		if (wm.function555()) num *= 2; else trash *= num ;
		if (wm.function556()) num *= 2; else trash *= num ;
		if (wm.function557()) num *= 2; else trash *= num ;
		if (wm.function558()) num *= 2; else trash *= num ;
		if (wm.function559()) num *= 2; else trash *= num ;
		if (wm.function560()) num *= 2; else trash *= num ;
		if (wm.function561()) num *= 2; else trash *= num ;
		if (wm.function562()) num *= 2; else trash *= num ;
		if (wm.function563()) num *= 2; else trash *= num ;
		if (wm.function564()) num *= 2; else trash *= num ;
		if (wm.function565()) num *= 2; else trash *= num ;
		if (wm.function566()) num *= 2; else trash *= num ;
		if (wm.function567()) num *= 2; else trash *= num ;
		if (wm.function568()) num *= 2; else trash *= num ;
		if (wm.function569()) num *= 2; else trash *= num ;
		if (wm.function570()) num *= 2; else trash *= num ;
		if (wm.function571()) num *= 2; else trash *= num ;
		if (wm.function572()) num *= 2; else trash *= num ;
		if (wm.function573()) num *= 2; else trash *= num ;
		if (wm.function574()) num *= 2; else trash *= num ;
		if (wm.function575()) num *= 2; else trash *= num ;
		if (wm.function576()) num *= 2; else trash *= num ;
		if (wm.function577()) num *= 2; else trash *= num ;
		if (wm.function578()) num *= 2; else trash *= num ;
		if (wm.function579()) num *= 2; else trash *= num ;
		if (wm.function580()) num *= 2; else trash *= num ;
		if (wm.function581()) num *= 2; else trash *= num ;
		if (wm.function582()) num *= 2; else trash *= num ;
		if (wm.function583()) num *= 2; else trash *= num ;
		if (wm.function584()) num *= 2; else trash *= num ;
		if (wm.function585()) num *= 2; else trash *= num ;
		if (wm.function586()) num *= 2; else trash *= num ;
		if (wm.function587()) num *= 2; else trash *= num ;
		if (wm.function588()) num *= 2; else trash *= num ;
		if (wm.function589()) num *= 2; else trash *= num ;
		if (wm.function590()) num *= 2; else trash *= num ;
		if (wm.function591()) num *= 2; else trash *= num ;
		if (wm.function592()) num *= 2; else trash *= num ;
		if (wm.function593()) num *= 2; else trash *= num ;
		if (wm.function594()) num *= 2; else trash *= num ;
		if (wm.function595()) num *= 2; else trash *= num ;
		if (wm.function596()) num *= 2; else trash *= num ;
		if (wm.function597()) num *= 2; else trash *= num ;
		if (wm.function598()) num *= 2; else trash *= num ;
		if (wm.function599()) num *= 2; else trash *= num ;
		if (wm.function600()) num *= 2; else trash *= num ;
		if (wm.function601()) num *= 2; else trash *= num ;
		if (wm.function602()) num *= 2; else trash *= num ;
		if (wm.function603()) num *= 2; else trash *= num ;
		if (wm.function604()) num *= 2; else trash *= num ;
		if (wm.function605()) num *= 2; else trash *= num ;
		if (wm.function606()) num *= 2; else trash *= num ;
		if (wm.function607()) num *= 2; else trash *= num ;
		if (wm.function608()) num *= 2; else trash *= num ;
		if (wm.function609()) num *= 2; else trash *= num ;
		if (wm.function610()) num *= 2; else trash *= num ;
		if (wm.function611()) num *= 2; else trash *= num ;
		if (wm.function612()) num *= 2; else trash *= num ;
		if (wm.function613()) num *= 2; else trash *= num ;
		if (wm.function614()) num *= 2; else trash *= num ;
		if (wm.function615()) num *= 2; else trash *= num ;
		if (wm.function616()) num *= 2; else trash *= num ;
		if (wm.function617()) num *= 2; else trash *= num ;
		if (wm.function618()) num *= 2; else trash *= num ;
		if (wm.function619()) num *= 2; else trash *= num ;
		if (wm.function620()) num *= 2; else trash *= num ;
		if (wm.function621()) num *= 2; else trash *= num ;
		if (wm.function622()) num *= 2; else trash *= num ;
		if (wm.function623()) num *= 2; else trash *= num ;
		if (wm.function624()) num *= 2; else trash *= num ;
		if (wm.function625()) num *= 2; else trash *= num ;
		if (wm.function626()) num *= 2; else trash *= num ;
		if (wm.function627()) num *= 2; else trash *= num ;
		if (wm.function628()) num *= 2; else trash *= num ;
		if (wm.function629()) num *= 2; else trash *= num ;
		if (wm.function630()) num *= 2; else trash *= num ;
		if (wm.function631()) num *= 2; else trash *= num ;
		if (wm.function632()) num *= 2; else trash *= num ;
		if (wm.function633()) num *= 2; else trash *= num ;
		if (wm.function634()) num *= 2; else trash *= num ;
		if (wm.function635()) num *= 2; else trash *= num ;
		if (wm.function636()) num *= 2; else trash *= num ;
		if (wm.function637()) num *= 2; else trash *= num ;
		if (wm.function638()) num *= 2; else trash *= num ;
		if (wm.function639()) num *= 2; else trash *= num ;
		if (wm.function640()) num *= 2; else trash *= num ;
		if (wm.function641()) num *= 2; else trash *= num ;
		if (wm.function642()) num *= 2; else trash *= num ;
		if (wm.function643()) num *= 2; else trash *= num ;
		if (wm.function644()) num *= 2; else trash *= num ;
		if (wm.function645()) num *= 2; else trash *= num ;
		if (wm.function646()) num *= 2; else trash *= num ;
		if (wm.function647()) num *= 2; else trash *= num ;
		if (wm.function648()) num *= 2; else trash *= num ;
		if (wm.function649()) num *= 2; else trash *= num ;
		if (wm.function650()) num *= 2; else trash *= num ;
		if (wm.function651()) num *= 2; else trash *= num ;
		if (wm.function652()) num *= 2; else trash *= num ;
		if (wm.function653()) num *= 2; else trash *= num ;
		if (wm.function654()) num *= 2; else trash *= num ;
		if (wm.function655()) num *= 2; else trash *= num ;
		if (wm.function656()) num *= 2; else trash *= num ;
		if (wm.function657()) num *= 2; else trash *= num ;
		if (wm.function658()) num *= 2; else trash *= num ;
		if (wm.function659()) num *= 2; else trash *= num ;
		if (wm.function660()) num *= 2; else trash *= num ;
		if (wm.function661()) num *= 2; else trash *= num ;
		if (wm.function662()) num *= 2; else trash *= num ;
		if (wm.function663()) num *= 2; else trash *= num ;
		if (wm.function664()) num *= 2; else trash *= num ;
		if (wm.function665()) num *= 2; else trash *= num ;
		if (wm.function666()) num *= 2; else trash *= num ;
		if (wm.function667()) num *= 2; else trash *= num ;
		if (wm.function668()) num *= 2; else trash *= num ;
		if (wm.function669()) num *= 2; else trash *= num ;
		if (wm.function670()) num *= 2; else trash *= num ;
		if (wm.function671()) num *= 2; else trash *= num ;
		if (wm.function672()) num *= 2; else trash *= num ;
		if (wm.function673()) num *= 2; else trash *= num ;
		if (wm.function674()) num *= 2; else trash *= num ;
		if (wm.function675()) num *= 2; else trash *= num ;
		if (wm.function676()) num *= 2; else trash *= num ;
		if (wm.function677()) num *= 2; else trash *= num ;
		if (wm.function678()) num *= 2; else trash *= num ;
		if (wm.function679()) num *= 2; else trash *= num ;
		if (wm.function680()) num *= 2; else trash *= num ;
		if (wm.function681()) num *= 2; else trash *= num ;
		if (wm.function682()) num *= 2; else trash *= num ;
		if (wm.function683()) num *= 2; else trash *= num ;
		if (wm.function684()) num *= 2; else trash *= num ;
		if (wm.function685()) num *= 2; else trash *= num ;
		if (wm.function686()) num *= 2; else trash *= num ;
		if (wm.function687()) num *= 2; else trash *= num ;
		if (wm.function688()) num *= 2; else trash *= num ;
		if (wm.function689()) num *= 2; else trash *= num ;
		if (wm.function690()) num *= 2; else trash *= num ;
		if (wm.function691()) num *= 2; else trash *= num ;
		if (wm.function692()) num *= 2; else trash *= num ;
		if (wm.function693()) num *= 2; else trash *= num ;
		if (wm.function694()) num *= 2; else trash *= num ;
		if (wm.function695()) num *= 2; else trash *= num ;
		if (wm.function696()) num *= 2; else trash *= num ;
		if (wm.function697()) num *= 2; else trash *= num ;
		if (wm.function698()) num *= 2; else trash *= num ;
		if (wm.function699()) num *= 2; else trash *= num ;
		if (wm.function700()) num *= 2; else trash *= num ;
		if (wm.function701()) num *= 2; else trash *= num ;
		if (wm.function702()) num *= 2; else trash *= num ;
		if (wm.function703()) num *= 2; else trash *= num ;
		if (wm.function704()) num *= 2; else trash *= num ;
		if (wm.function705()) num *= 2; else trash *= num ;
		if (wm.function706()) num *= 2; else trash *= num ;
		if (wm.function707()) num *= 2; else trash *= num ;
		if (wm.function708()) num *= 2; else trash *= num ;
		if (wm.function709()) num *= 2; else trash *= num ;
		if (wm.function710()) num *= 2; else trash *= num ;
		if (wm.function711()) num *= 2; else trash *= num ;
		if (wm.function712()) num *= 2; else trash *= num ;
		if (wm.function713()) num *= 2; else trash *= num ;
		if (wm.function714()) num *= 2; else trash *= num ;
		if (wm.function715()) num *= 2; else trash *= num ;
		if (wm.function716()) num *= 2; else trash *= num ;
		if (wm.function717()) num *= 2; else trash *= num ;
		if (wm.function718()) num *= 2; else trash *= num ;
		if (wm.function719()) num *= 2; else trash *= num ;
		if (wm.function720()) num *= 2; else trash *= num ;
		if (wm.function721()) num *= 2; else trash *= num ;
		if (wm.function722()) num *= 2; else trash *= num ;
		if (wm.function723()) num *= 2; else trash *= num ;
		if (wm.function724()) num *= 2; else trash *= num ;
		if (wm.function725()) num *= 2; else trash *= num ;
		if (wm.function726()) num *= 2; else trash *= num ;
		if (wm.function727()) num *= 2; else trash *= num ;
		if (wm.function728()) num *= 2; else trash *= num ;
		if (wm.function729()) num *= 2; else trash *= num ;
		if (wm.function730()) num *= 2; else trash *= num ;
		if (wm.function731()) num *= 2; else trash *= num ;
		if (wm.function732()) num *= 2; else trash *= num ;
		if (wm.function733()) num *= 2; else trash *= num ;
		if (wm.function734()) num *= 2; else trash *= num ;
		if (wm.function735()) num *= 2; else trash *= num ;
		if (wm.function736()) num *= 2; else trash *= num ;
		if (wm.function737()) num *= 2; else trash *= num ;
		if (wm.function738()) num *= 2; else trash *= num ;
		if (wm.function739()) num *= 2; else trash *= num ;
		if (wm.function740()) num *= 2; else trash *= num ;
		if (wm.function741()) num *= 2; else trash *= num ;
		if (wm.function742()) num *= 2; else trash *= num ;
		if (wm.function743()) num *= 2; else trash *= num ;
		if (wm.function744()) num *= 2; else trash *= num ;
		if (wm.function745()) num *= 2; else trash *= num ;
		if (wm.function746()) num *= 2; else trash *= num ;
		if (wm.function747()) num *= 2; else trash *= num ;
		if (wm.function748()) num *= 2; else trash *= num ;
		if (wm.function749()) num *= 2; else trash *= num ;
		if (wm.function750()) num *= 2; else trash *= num ;
		if (wm.function751()) num *= 2; else trash *= num ;
		if (wm.function752()) num *= 2; else trash *= num ;
		if (wm.function753()) num *= 2; else trash *= num ;
		if (wm.function754()) num *= 2; else trash *= num ;
		if (wm.function755()) num *= 2; else trash *= num ;
		if (wm.function756()) num *= 2; else trash *= num ;
		if (wm.function757()) num *= 2; else trash *= num ;
		if (wm.function758()) num *= 2; else trash *= num ;
		if (wm.function759()) num *= 2; else trash *= num ;
		if (wm.function760()) num *= 2; else trash *= num ;
		if (wm.function761()) num *= 2; else trash *= num ;
		if (wm.function762()) num *= 2; else trash *= num ;
		if (wm.function763()) num *= 2; else trash *= num ;
		if (wm.function764()) num *= 2; else trash *= num ;
		if (wm.function765()) num *= 2; else trash *= num ;
		if (wm.function766()) num *= 2; else trash *= num ;
		if (wm.function767()) num *= 2; else trash *= num ;
		if (wm.function768()) num *= 2; else trash *= num ;
		if (wm.function769()) num *= 2; else trash *= num ;
		if (wm.function770()) num *= 2; else trash *= num ;
		if (wm.function771()) num *= 2; else trash *= num ;
		if (wm.function772()) num *= 2; else trash *= num ;
		if (wm.function773()) num *= 2; else trash *= num ;
		if (wm.function774()) num *= 2; else trash *= num ;
		if (wm.function775()) num *= 2; else trash *= num ;
		if (wm.function776()) num *= 2; else trash *= num ;
		if (wm.function777()) num *= 2; else trash *= num ;
		if (wm.function778()) num *= 2; else trash *= num ;
		if (wm.function779()) num *= 2; else trash *= num ;
		if (wm.function780()) num *= 2; else trash *= num ;
		if (wm.function781()) num *= 2; else trash *= num ;
		if (wm.function782()) num *= 2; else trash *= num ;
		if (wm.function783()) num *= 2; else trash *= num ;
		if (wm.function784()) num *= 2; else trash *= num ;
		if (wm.function785()) num *= 2; else trash *= num ;
		if (wm.function786()) num *= 2; else trash *= num ;
		if (wm.function787()) num *= 2; else trash *= num ;
		if (wm.function788()) num *= 2; else trash *= num ;
		if (wm.function789()) num *= 2; else trash *= num ;
		if (wm.function790()) num *= 2; else trash *= num ;
		if (wm.function791()) num *= 2; else trash *= num ;
		if (wm.function792()) num *= 2; else trash *= num ;
		if (wm.function793()) num *= 2; else trash *= num ;
		if (wm.function794()) num *= 2; else trash *= num ;
		if (wm.function795()) num *= 2; else trash *= num ;
		if (wm.function796()) num *= 2; else trash *= num ;
		if (wm.function797()) num *= 2; else trash *= num ;
		if (wm.function798()) num *= 2; else trash *= num ;
		if (wm.function799()) num *= 2; else trash *= num ;
		if (wm.function800()) num *= 2; else trash *= num ;
		if (wm.function801()) num *= 2; else trash *= num ;
		if (wm.function802()) num *= 2; else trash *= num ;
		if (wm.function803()) num *= 2; else trash *= num ;
		if (wm.function804()) num *= 2; else trash *= num ;
		if (wm.function805()) num *= 2; else trash *= num ;
		if (wm.function806()) num *= 2; else trash *= num ;
		if (wm.function807()) num *= 2; else trash *= num ;
		if (wm.function808()) num *= 2; else trash *= num ;
		if (wm.function809()) num *= 2; else trash *= num ;
		if (wm.function810()) num *= 2; else trash *= num ;
		if (wm.function811()) num *= 2; else trash *= num ;
		if (wm.function812()) num *= 2; else trash *= num ;
		if (wm.function813()) num *= 2; else trash *= num ;
		if (wm.function814()) num *= 2; else trash *= num ;
		if (wm.function815()) num *= 2; else trash *= num ;
		if (wm.function816()) num *= 2; else trash *= num ;
		if (wm.function817()) num *= 2; else trash *= num ;
		if (wm.function818()) num *= 2; else trash *= num ;
		if (wm.function819()) num *= 2; else trash *= num ;
		if (wm.function820()) num *= 2; else trash *= num ;
		if (wm.function821()) num *= 2; else trash *= num ;
		if (wm.function822()) num *= 2; else trash *= num ;
		if (wm.function823()) num *= 2; else trash *= num ;
		if (wm.function824()) num *= 2; else trash *= num ;
		if (wm.function825()) num *= 2; else trash *= num ;
		if (wm.function826()) num *= 2; else trash *= num ;
		if (wm.function827()) num *= 2; else trash *= num ;
		if (wm.function828()) num *= 2; else trash *= num ;
		if (wm.function829()) num *= 2; else trash *= num ;
		if (wm.function830()) num *= 2; else trash *= num ;
		if (wm.function831()) num *= 2; else trash *= num ;
		if (wm.function832()) num *= 2; else trash *= num ;
		if (wm.function833()) num *= 2; else trash *= num ;
		if (wm.function834()) num *= 2; else trash *= num ;
		if (wm.function835()) num *= 2; else trash *= num ;
		if (wm.function836()) num *= 2; else trash *= num ;
		if (wm.function837()) num *= 2; else trash *= num ;
		if (wm.function838()) num *= 2; else trash *= num ;
		if (wm.function839()) num *= 2; else trash *= num ;
		if (wm.function840()) num *= 2; else trash *= num ;
		if (wm.function841()) num *= 2; else trash *= num ;
		if (wm.function842()) num *= 2; else trash *= num ;
		if (wm.function843()) num *= 2; else trash *= num ;
		if (wm.function844()) num *= 2; else trash *= num ;
		if (wm.function845()) num *= 2; else trash *= num ;
		if (wm.function846()) num *= 2; else trash *= num ;
		if (wm.function847()) num *= 2; else trash *= num ;
		if (wm.function848()) num *= 2; else trash *= num ;
		if (wm.function849()) num *= 2; else trash *= num ;
		if (wm.function850()) num *= 2; else trash *= num ;
		if (wm.function851()) num *= 2; else trash *= num ;
		if (wm.function852()) num *= 2; else trash *= num ;
		if (wm.function853()) num *= 2; else trash *= num ;
		if (wm.function854()) num *= 2; else trash *= num ;
		if (wm.function855()) num *= 2; else trash *= num ;
		if (wm.function856()) num *= 2; else trash *= num ;
		if (wm.function857()) num *= 2; else trash *= num ;
		if (wm.function858()) num *= 2; else trash *= num ;
		if (wm.function859()) num *= 2; else trash *= num ;
		if (wm.function860()) num *= 2; else trash *= num ;
		if (wm.function861()) num *= 2; else trash *= num ;
		if (wm.function862()) num *= 2; else trash *= num ;
		if (wm.function863()) num *= 2; else trash *= num ;
		if (wm.function864()) num *= 2; else trash *= num ;
		if (wm.function865()) num *= 2; else trash *= num ;
		if (wm.function866()) num *= 2; else trash *= num ;
		if (wm.function867()) num *= 2; else trash *= num ;
		if (wm.function868()) num *= 2; else trash *= num ;
		if (wm.function869()) num *= 2; else trash *= num ;
		if (wm.function870()) num *= 2; else trash *= num ;
		if (wm.function871()) num *= 2; else trash *= num ;
		if (wm.function872()) num *= 2; else trash *= num ;
		if (wm.function873()) num *= 2; else trash *= num ;
		if (wm.function874()) num *= 2; else trash *= num ;
		if (wm.function875()) num *= 2; else trash *= num ;
		if (wm.function876()) num *= 2; else trash *= num ;
		if (wm.function877()) num *= 2; else trash *= num ;
		if (wm.function878()) num *= 2; else trash *= num ;
		if (wm.function879()) num *= 2; else trash *= num ;
		if (wm.function880()) num *= 2; else trash *= num ;
		if (wm.function881()) num *= 2; else trash *= num ;
		if (wm.function882()) num *= 2; else trash *= num ;
		if (wm.function883()) num *= 2; else trash *= num ;
		if (wm.function884()) num *= 2; else trash *= num ;
		if (wm.function885()) num *= 2; else trash *= num ;
		if (wm.function886()) num *= 2; else trash *= num ;
		if (wm.function887()) num *= 2; else trash *= num ;
		if (wm.function888()) num *= 2; else trash *= num ;
		if (wm.function889()) num *= 2; else trash *= num ;
		if (wm.function890()) num *= 2; else trash *= num ;
		if (wm.function891()) num *= 2; else trash *= num ;
		if (wm.function892()) num *= 2; else trash *= num ;
		if (wm.function893()) num *= 2; else trash *= num ;
		if (wm.function894()) num *= 2; else trash *= num ;
		if (wm.function895()) num *= 2; else trash *= num ;
		if (wm.function896()) num *= 2; else trash *= num ;
		if (wm.function897()) num *= 2; else trash *= num ;
		if (wm.function898()) num *= 2; else trash *= num ;
		if (wm.function899()) num *= 2; else trash *= num ;
		if (wm.function900()) num *= 2; else trash *= num ;
		if (wm.function901()) num *= 2; else trash *= num ;
		if (wm.function902()) num *= 2; else trash *= num ;
		if (wm.function903()) num *= 2; else trash *= num ;
		if (wm.function904()) num *= 2; else trash *= num ;
		if (wm.function905()) num *= 2; else trash *= num ;
		if (wm.function906()) num *= 2; else trash *= num ;
		if (wm.function907()) num *= 2; else trash *= num ;
		if (wm.function908()) num *= 2; else trash *= num ;
		if (wm.function909()) num *= 2; else trash *= num ;
		if (wm.function910()) num *= 2; else trash *= num ;
		if (wm.function911()) num *= 2; else trash *= num ;
		if (wm.function912()) num *= 2; else trash *= num ;
		if (wm.function913()) num *= 2; else trash *= num ;
		if (wm.function914()) num *= 2; else trash *= num ;
		if (wm.function915()) num *= 2; else trash *= num ;
		if (wm.function916()) num *= 2; else trash *= num ;
		if (wm.function917()) num *= 2; else trash *= num ;
		if (wm.function918()) num *= 2; else trash *= num ;
		if (wm.function919()) num *= 2; else trash *= num ;
		if (wm.function920()) num *= 2; else trash *= num ;
		if (wm.function921()) num *= 2; else trash *= num ;
		if (wm.function922()) num *= 2; else trash *= num ;
		if (wm.function923()) num *= 2; else trash *= num ;
		if (wm.function924()) num *= 2; else trash *= num ;
		if (wm.function925()) num *= 2; else trash *= num ;
		if (wm.function926()) num *= 2; else trash *= num ;
		if (wm.function927()) num *= 2; else trash *= num ;
		if (wm.function928()) num *= 2; else trash *= num ;
		if (wm.function929()) num *= 2; else trash *= num ;
		if (wm.function930()) num *= 2; else trash *= num ;
		if (wm.function931()) num *= 2; else trash *= num ;
		if (wm.function932()) num *= 2; else trash *= num ;
		if (wm.function933()) num *= 2; else trash *= num ;
		if (wm.function934()) num *= 2; else trash *= num ;
		if (wm.function935()) num *= 2; else trash *= num ;
		if (wm.function936()) num *= 2; else trash *= num ;
		if (wm.function937()) num *= 2; else trash *= num ;
		if (wm.function938()) num *= 2; else trash *= num ;
		if (wm.function939()) num *= 2; else trash *= num ;
		if (wm.function940()) num *= 2; else trash *= num ;
		if (wm.function941()) num *= 2; else trash *= num ;
		if (wm.function942()) num *= 2; else trash *= num ;
		if (wm.function943()) num *= 2; else trash *= num ;
		if (wm.function944()) num *= 2; else trash *= num ;
		if (wm.function945()) num *= 2; else trash *= num ;
		if (wm.function946()) num *= 2; else trash *= num ;
		if (wm.function947()) num *= 2; else trash *= num ;
		if (wm.function948()) num *= 2; else trash *= num ;
		if (wm.function949()) num *= 2; else trash *= num ;
		if (wm.function950()) num *= 2; else trash *= num ;
		if (wm.function951()) num *= 2; else trash *= num ;
		if (wm.function952()) num *= 2; else trash *= num ;
		if (wm.function953()) num *= 2; else trash *= num ;
		if (wm.function954()) num *= 2; else trash *= num ;
		if (wm.function955()) num *= 2; else trash *= num ;
		if (wm.function956()) num *= 2; else trash *= num ;
		if (wm.function957()) num *= 2; else trash *= num ;
		if (wm.function958()) num *= 2; else trash *= num ;
		if (wm.function959()) num *= 2; else trash *= num ;
		if (wm.function960()) num *= 2; else trash *= num ;
		if (wm.function961()) num *= 2; else trash *= num ;
		if (wm.function962()) num *= 2; else trash *= num ;
		if (wm.function963()) num *= 2; else trash *= num ;
		if (wm.function964()) num *= 2; else trash *= num ;
		if (wm.function965()) num *= 2; else trash *= num ;
		if (wm.function966()) num *= 2; else trash *= num ;
		if (wm.function967()) num *= 2; else trash *= num ;
		if (wm.function968()) num *= 2; else trash *= num ;
		if (wm.function969()) num *= 2; else trash *= num ;
		if (wm.function970()) num *= 2; else trash *= num ;
		if (wm.function971()) num *= 2; else trash *= num ;
		if (wm.function972()) num *= 2; else trash *= num ;
		if (wm.function973()) num *= 2; else trash *= num ;
		if (wm.function974()) num *= 2; else trash *= num ;
		if (wm.function975()) num *= 2; else trash *= num ;
		if (wm.function976()) num *= 2; else trash *= num ;
		if (wm.function977()) num *= 2; else trash *= num ;
		if (wm.function978()) num *= 2; else trash *= num ;
		if (wm.function979()) num *= 2; else trash *= num ;
		if (wm.function980()) num *= 2; else trash *= num ;
		if (wm.function981()) num *= 2; else trash *= num ;
		if (wm.function982()) num *= 2; else trash *= num ;
		if (wm.function983()) num *= 2; else trash *= num ;
		if (wm.function984()) num *= 2; else trash *= num ;
		if (wm.function985()) num *= 2; else trash *= num ;
		if (wm.function986()) num *= 2; else trash *= num ;
		if (wm.function987()) num *= 2; else trash *= num ;
		if (wm.function988()) num *= 2; else trash *= num ;
		if (wm.function989()) num *= 2; else trash *= num ;
		if (wm.function990()) num *= 2; else trash *= num ;
		if (wm.function991()) num *= 2; else trash *= num ;
		if (wm.function992()) num *= 2; else trash *= num ;
		if (wm.function993()) num *= 2; else trash *= num ;
		if (wm.function994()) num *= 2; else trash *= num ;
		if (wm.function995()) num *= 2; else trash *= num ;
		if (wm.function996()) num *= 2; else trash *= num ;
		if (wm.function997()) num *= 2; else trash *= num ;
		if (wm.function998()) num *= 2; else trash *= num ;
		if (wm.function999()) num *= 2; else trash *= num ;
		if (wm.function1000()) num *= 2; else trash *= num ;
		
		
		System.out.format("Num Value After : %d\n", num);
	}
	
	public static void main(String args[]) {
		Watch watch = new Watch();
		watch.begin();
	}
}
